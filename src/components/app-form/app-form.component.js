import template from './app-form.html';
import controller from './app-form.controller';

let component = {
  restrict: 'E',
  scope: {},
  template,
  controller,
  controllerAs: 'vm'
};

export default component;