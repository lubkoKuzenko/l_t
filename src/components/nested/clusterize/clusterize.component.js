import template from './clusterize.html';
import controller from './clusterize.controller';

let ClusterizeComponent = {
  restrict: 'E',
  scope: {},
  template,
  controller,
  controllerAs: 'vm',
  // bindings: {
  //    rows: '<'
  // }
};

export default ClusterizeComponent;