import template from './chart.html';
import controller from './chart.controller';

let chartComponent = {
  restrict: 'E',
  scope: {},
  template,
  controller,
  controllerAs: 'vm'
};

export default chartComponent;